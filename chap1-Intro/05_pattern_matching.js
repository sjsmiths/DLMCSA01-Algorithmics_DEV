/* TODO port to  a console-dialog

  var t = prompt("Short present tense sentence with a pronoun and a verb");
  var pat3s = /^(He|She|It)\s.*?s$/; // Pattern for the singular 3rd person
  var patOp = /^(I|You|We|They)\s.*?[^s]$/; //Pattern for all other persons
  if ((t.search(pat3s)!=0)&&(t.search(patOp)!=0)){
    document.write("Sentence " + t + " is grammatically incorrect");
    }
    else{
    document.write("Sentence " + t + " is grammatically correct");
    }
 */